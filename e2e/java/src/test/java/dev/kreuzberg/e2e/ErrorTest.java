// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bfe6bd55b30574045ea068c1ccb2de665f5a3b884a98106adf1dfed6301442c7
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.Kreuzberg;
import dev.kreuzberg.ExtractionConfig;
import java.util.Optional;

/** E2e tests for category: error. */
class ErrorTest {
    @Test
    void testErrorEmptyBytes() throws Exception {
        // Graceful handling of empty bytes (should not error)
        var result = Kreuzberg.extractBytesSync("text/empty.txt".getBytes(), "text/plain", ExtractionConfig.builder().build());
    }

    @Test
    void testErrorEmptyMime() throws Exception {
        // Error when extracting with empty MIME type
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/plain.txt".getBytes(), "", ExtractionConfig.builder().build()));
    }

    @Test
    void testErrorExtractBytesConflictingOcr() throws Exception {
        // extract_bytes force+disable OCR
        var config = ExtractionConfig.builder().withDisableOcr(true).withForceOcr(true).build();
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/fake_text.txt".getBytes(), "text/plain", config));
    }

    @Test
    void testErrorInvalidMimeFormat() throws Exception {
        // Error when extracting with invalid MIME type format
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/plain.txt".getBytes(), "not-a-mime", ExtractionConfig.builder().build()));
    }

    @Test
    void testErrorUnsupportedMime() throws Exception {
        // Error when extracting with unsupported MIME type
        assertThrows(Exception.class, () -> Kreuzberg.extractBytesSync("text/plain.txt".getBytes(), "application/x-nonexistent", ExtractionConfig.builder().build()));
    }

}
