// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c33e5123eb813279c51bed4e2dd6bab0623d2d7c17fc42f80394bae519956216
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
#nullable enable

using System.Text.Json.Serialization;

namespace Kreuzberg;

/// <summary>
/// Format-specific metadata (discriminated union).
///
/// Only one format type can exist per extraction result. This provides
/// type-safe, clean metadata without nested optionals.
/// </summary>
[JsonPolymorphic(TypeDiscriminatorPropertyName = "format_type")]
public abstract record FormatMetadata
{
    [JsonDerivedType(typeof(Pdf), "pdf")]
    public sealed record Pdf(
        string Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Docx), "docx")]
    public sealed record Docx(
        DocxMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Excel), "excel")]
    public sealed record Excel(
        ExcelMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Email), "email")]
    public sealed record Email(
        EmailMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Pptx), "pptx")]
    public sealed record Pptx(
        PptxMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Archive), "archive")]
    public sealed record Archive(
        ArchiveMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Image), "image")]
    public sealed record Image(
        string Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Xml), "xml")]
    public sealed record Xml(
        XmlMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Text), "text")]
    public sealed record Text(
        TextMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Html), "html")]
    public sealed record Html(
        HtmlMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Ocr), "ocr")]
    public sealed record Ocr(
        OcrMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Csv), "csv")]
    public sealed record Csv(
        CsvMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Structured), "structured")]
    public sealed record Structured(
        StructuredMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Bibtex), "bibtex")]
    public sealed record Bibtex(
        BibtexMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Citation), "citation")]
    public sealed record Citation(
        CitationMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(FictionBook), "fiction_book")]
    public sealed record FictionBook(
        FictionBookMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Dbf), "dbf")]
    public sealed record Dbf(
        DbfMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Jats), "jats")]
    public sealed record Jats(
        JatsMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Epub), "epub")]
    public sealed record Epub(
        EpubMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Pst), "pst")]
    public sealed record Pst(
        PstMetadata Value
    ) : FormatMetadata;

    [JsonDerivedType(typeof(Code), "code")]
    public sealed record Code(
        string Value
    ) : FormatMetadata;

    /// <summary>Returns the Pdf data if this is a Pdf variant, otherwise null.</summary>
    public string? Pdf => this is Pdf e ? e.Value : null;

    /// <summary>Returns the Docx data if this is a Docx variant, otherwise null.</summary>
    public DocxMetadata? Docx => this is Docx e ? e.Value : null;

    /// <summary>Returns the Excel data if this is a Excel variant, otherwise null.</summary>
    public ExcelMetadata? Excel => this is Excel e ? e.Value : null;

    /// <summary>Returns the Email data if this is a Email variant, otherwise null.</summary>
    public EmailMetadata? Email => this is Email e ? e.Value : null;

    /// <summary>Returns the Pptx data if this is a Pptx variant, otherwise null.</summary>
    public PptxMetadata? Pptx => this is Pptx e ? e.Value : null;

    /// <summary>Returns the Archive data if this is a Archive variant, otherwise null.</summary>
    public ArchiveMetadata? Archive => this is Archive e ? e.Value : null;

    /// <summary>Returns the Image data if this is a Image variant, otherwise null.</summary>
    public string? Image => this is Image e ? e.Value : null;

    /// <summary>Returns the Xml data if this is a Xml variant, otherwise null.</summary>
    public XmlMetadata? Xml => this is Xml e ? e.Value : null;

    /// <summary>Returns the Text data if this is a Text variant, otherwise null.</summary>
    public TextMetadata? Text => this is Text e ? e.Value : null;

    /// <summary>Returns the Html data if this is a Html variant, otherwise null.</summary>
    public HtmlMetadata? Html => this is Html e ? e.Value : null;

    /// <summary>Returns the Ocr data if this is a Ocr variant, otherwise null.</summary>
    public OcrMetadata? Ocr => this is Ocr e ? e.Value : null;

    /// <summary>Returns the Csv data if this is a Csv variant, otherwise null.</summary>
    public CsvMetadata? Csv => this is Csv e ? e.Value : null;

    /// <summary>Returns the Structured data if this is a Structured variant, otherwise null.</summary>
    public StructuredMetadata? Structured => this is Structured e ? e.Value : null;

    /// <summary>Returns the Bibtex data if this is a Bibtex variant, otherwise null.</summary>
    public BibtexMetadata? Bibtex => this is Bibtex e ? e.Value : null;

    /// <summary>Returns the Citation data if this is a Citation variant, otherwise null.</summary>
    public CitationMetadata? Citation => this is Citation e ? e.Value : null;

    /// <summary>Returns the FictionBook data if this is a FictionBook variant, otherwise null.</summary>
    public FictionBookMetadata? FictionBook => this is FictionBook e ? e.Value : null;

    /// <summary>Returns the Dbf data if this is a Dbf variant, otherwise null.</summary>
    public DbfMetadata? Dbf => this is Dbf e ? e.Value : null;

    /// <summary>Returns the Jats data if this is a Jats variant, otherwise null.</summary>
    public JatsMetadata? Jats => this is Jats e ? e.Value : null;

    /// <summary>Returns the Epub data if this is a Epub variant, otherwise null.</summary>
    public EpubMetadata? Epub => this is Epub e ? e.Value : null;

    /// <summary>Returns the Pst data if this is a Pst variant, otherwise null.</summary>
    public PstMetadata? Pst => this is Pst e ? e.Value : null;

    /// <summary>Returns the Code data if this is a Code variant, otherwise null.</summary>
    public string? Code => this is Code e ? e.Value : null;

}
