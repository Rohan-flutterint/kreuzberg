// Generated by alef. Do not edit by hand.

package dev.kreuzberg

import java.nio.file.Path

/** JNI-backed wrapper holding a native `HwpxExtractor` handle. */
class HwpxExtractor internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    fun name(): String {
        return KreuzbergBridge.nativeHwpxExtractorName(handle)
    }

    fun version(): String {
        return KreuzbergBridge.nativeHwpxExtractorVersion(handle)
    }

    fun initialize(): Unit {
        KreuzbergBridge.nativeHwpxExtractorInitialize(handle)
    }

    fun shutdown(): Unit {
        KreuzbergBridge.nativeHwpxExtractorShutdown(handle)
    }

    fun description(): String {
        return KreuzbergBridge.nativeHwpxExtractorDescription(handle)
    }

    fun author(): String {
        return KreuzbergBridge.nativeHwpxExtractorAuthor(handle)
    }

    fun supportedMimeTypes(): List<String> {
        val responseJson = KreuzbergBridge.nativeHwpxExtractorSupportedMimeTypes(handle)
        return MAPPER.readValue(responseJson, List<String>::class.java)
    }

    fun priority(): Int {
        return KreuzbergBridge.nativeHwpxExtractorPriority(handle)
    }

    override fun close() { KreuzbergBridge.nativeFreeHwpxExtractor(handle) }
}
/** JNI-backed wrapper holding a native `TessdataManager` handle. */
class TessdataManager internal constructor(internal val handle: Long) : AutoCloseable {
    companion object {
        private val MAPPER = com.fasterxml.jackson.databind.ObjectMapper()
            .registerModule(com.fasterxml.jackson.datatype.jdk8.Jdk8Module())
            .findAndRegisterModules()
            .setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }

    // Get the cache directory path.
    fun cacheDir(): Path {
        return KreuzbergBridge.nativeTessdataManagerCacheDir(handle)
    }

    // Check if a specific language traineddata file is cached.
    fun isLanguageCached(lang: String): Boolean {
        return KreuzbergBridge.nativeTessdataManagerIsLanguageCached(handle, MAPPER.writeValueAsString(lang))
    }

    // Downloads all tessdata_fast traineddata files to the cache directory.
    //
    // Skips files that already exist. Returns the count of newly downloaded files.
    //
    // When the `paddle-ocr` feature is not enabled, no download URLs are available
    // and this method always returns `Ok(0)`.
    fun ensureAllLanguages(): Long {
        return KreuzbergBridge.nativeTessdataManagerEnsureAllLanguages(handle)
    }

    override fun close() { KreuzbergBridge.nativeFreeTessdataManager(handle) }
}
